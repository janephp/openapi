swagger: "2.0"
paths:
  /test:
    post:
      operationId: "test"
      parameters:
        - name: "body"
          in: "body"
          required: true
          schema:
            type: "object"
            properties:
              foo:
                type: "string"
